package coschat.wecanstudio.coschat.utils;

import android.databinding.BindingAdapter;
import android.databinding.ObservableArrayList;
import android.graphics.Typeface;
import android.support.v7.widget.LinearLayoutManager;
import android.support.v7.widget.RecyclerView;
import android.widget.ImageView;
import android.widget.TextView;

import coschat.wecanstudio.coschat.R;
import coschat.wecanstudio.coschat.model.Users;

/**
 * Created by sumeet on 19/7/16.
 */
public class BindingUtils {

    /*
    fresco预留
    @BindingAdapter({"imageUrl"})
    public static void loadImage(ImageView imageView, String url) {
        if (url == null) {
            imageView.setImageResource(R.mipmap.ic_launcher);
        } else {
           fresco.with(imageView.getContext()).load(url).into(imageView);
        }
    }
*/
}
